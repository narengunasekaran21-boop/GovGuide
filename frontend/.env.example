# Not required for local dev (Vite proxies /api to the backend, see vite.config.js).
# Included for reference if you later deploy frontend and backend separately.
VITE_API_BASE_URL=http://localhost:5000/api
